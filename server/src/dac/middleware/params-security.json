
{
    "/api/auth/login": {
        "username": { "required": true, "type": "string", "max": 50 }, 
        "password": { "required": true, "type": "string", "max": 100 },
        "skipLastAccess" : true

    },

    "/api/auth/session": {
        "skipLastAccess" : true
    }
}
